import test from 'ava'
import checker from './cli.js'

// Will take care of tests afterwards - design first.

test('should wait until api response before exiting', async t => {
  t.pass();
});

test('Should effectively wait handle async call', t => {
  t.pass();
});

test('Should get response before 2.5 * 1000 ms', t => {
  t.pass();
});

test('Should return the right number of bikes available upon query', t => {
  t.pass();
});

test('Should return the right number of docks available upon query', t => {
  t.pass();
});

test('Should return the same length for list of ids and names', t => {
  t.pass();
});

test('Should return accurate list of ids and names', t => {

});
